/**
 * @deprecated Import from `@/lib/reputation/lookup` instead.
 * Kept so older imports keep working.
 */
export {
  fetchFaceitBySteamId,
  lookupPlayerReputation,
} from "@/lib/reputation/lookup";
