# SQLite migration archive

These migrations applied to the previous local SQLite database (`file:./dev.db`).
They are kept for historical reference only.

The active provider is **PostgreSQL** (Supabase). See `prisma/migrations/20260728120000_init_postgres/`.
